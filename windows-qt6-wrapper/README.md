I can't get this working with static qt5 builds on windows, so instead I just have it install msys2 and run it through that
a build of this project goes into the melonDS-plugins folder then you run start.bat and it'll download msys2 with melonDS dependencies to msys2/root
